# Under construction 

cd flaskproject
docker build -t flaskproject . # собираем образ
docker run -d -p 5000:5000 --name flask --hostname flask.docker -v "$(pwd)"/app:/app  flaskproject # запускаем контейнер, монтируя директорию app внутрь
